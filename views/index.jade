extends layout

block content
        .ml(ng-controller="mlcontroller")
            h1 Melisa
            input(placeholder="Hola ¿Qué querés saber sobre este producto?", ng-model="query" ng-change="askMelisa(query)" ng-model-options='{ debounce: 1000 }' type="text")
            ul
                li(id="resp")
                  {{resp}}
                  li
                    {{price | currency}}
