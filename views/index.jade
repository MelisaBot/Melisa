extends layout

block content
        .ml(ng-controller="mlcontroller")
            h1 Melisa
            input(placeholder="Hola ¿Qué querés saber sobre este producto?", ng-model="query" ng-change="askMelisa(query)" type="text")
            ul
                li(id="resp")
                  {{resp}}
